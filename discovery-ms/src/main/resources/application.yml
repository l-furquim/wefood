server:
  port: 8084
spring:
  cloud:
    config:
      uri: http://127.0.0.1:8889
      enabled: false
  application:
    name: discovery-ms
eureka:
  client:
    register-with-eureka: true
    fetch-registry: false

